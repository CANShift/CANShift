# macOS
.DS_Store
**/.DS_Store

# IDE
.vscode/settings.json
.idea/

# Claude
.claude/

# Environment — never commit secrets
.env
.env.*
*.env
CLAUDE.md

# Dependencies — each package has its own .gitignore but belt-and-suspenders
**/node_modules

# Local docs — keep on disk, never push
*.pdf
3D
